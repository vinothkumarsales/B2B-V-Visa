import{notFound}from'next/navigation';import{CatalogueControlPanel}from'@/components/admin/CatalogueControlPanel';import{getCataloguePageData}from'@/server/admin/catalogue-page-data';export const dynamic='force-dynamic';export default async function Page({params}:{params:Promise<{productId:string}>}){const{productId}=await params;const data=await getCataloguePageData();const product=data.products.find(item=>item.id===productId);if(!product)notFound();return <CatalogueControlPanel countries={data.countries} products={[product]} initialTab="product"/>}
